<?phx

if ($argc > 1) {
	var_dump($argv);
}

function getIds()
{
    return [3,4,5];
}

class Provider
{
    public function getNames(): array { return ['Pascal', 'Philipp']; }

    public static function getOtherNames(): array { return ['Cassandra']; }
}

// Results in array: ['foo', 'hello', 'my', 'world', 3, 4, 5, 'bar', 'baz']
$crazy = ['my'];
$otherItems = ['hello', ...$crazy, 'world'];
$otherItems2 = ['baz'];
//$items = ['foo', ...$otherItems, ...getIds(), 'bar', ...$otherItems2];

$provider = new Provider();

// This needs recursive thinking
$items = ['foo', ...$otherItems, ...([...$crazy, 'meow', ...getIds(), ...$provider->getNames()]), 'bar', ...$otherItems2, ...Provider::getOtherNames()];


// for (... in ...)
for ($item in $items) {
	echo $item , PHP_EOL;
}

//[$itemOne, ...$others, $secondLast, $last] = $items;
